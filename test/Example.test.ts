import { expect } from "chai";

describe("CLASS_TO_TEST", () => {
  it("should do test 1...", () => {
    expect(1).to.equal(1);
  });
  it("should do test 2...", () => {
    expect([1, 2, 3]).to.eql(2);
  });
});
// TODO: add tests
